import React from 'react';
import { StyleSheet } from 'react-native';
import { View, Center, Text, Button } from 'native-base';

export const ProductListHeader = () => {
    return (
        <Center>
            <Text>Product List Header</Text>
        </Center>
    );
};